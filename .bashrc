#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '


#[dalter124@Mahoraga143 .dotfiles]$ ln -s ~/.
# dotfiles/.zshrc ~/.zshrc
##[dalter124@Mahoraga143 .dotfiles]$ ln -s ~/.
# dotfiles/.gitconfig ~/.gitconfig
